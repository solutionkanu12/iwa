# Secrets. Never commit these.
.env
.env.local
.env.*.local

# Per-run state: circle id and submitted transaction hashes. Regenerated by the
# run itself; the durable record is strk20.json.
run-state.json

# Scratch
_*.mjs
